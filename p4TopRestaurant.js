function topRatedRestaurant(restaurants) {
    const topRatings = Object.values(restaurants.rating);
    if (Array.isArray(restaurants) && restaurants.length  !== 0) 
    {
        for(let elem of Object.values(restaurants.rating))
        {
            
        }

    }
}
